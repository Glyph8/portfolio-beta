# CLAUDE.md

이 문서는 Claude Code가 본 저장소에서 작업할 때 참조하는 가이드라인입니다. 모든 코드 생성, 수정, 리뷰 작업은 이 문서에 명시된 원칙을 따라야 합니다.

---

## 1. 프로젝트 정체성

- **프로젝트명:** 강동윤(Dongyun) 개발자 웹 포트폴리오
- **타겟 독자:** 시니어 개발자, 기술 면접관, 채용 담당자
- **핵심 메시지:** 보안(E2EE)과 인프라(배포/DB) 아키텍처를 깊이 있게 이해하고, 이를 사용자 친화적인 클라이언트(Next.js)로 구현해내는 **T자형 풀스택 엔지니어**

### 디자인 3원칙 (모든 작업의 판단 기준)

1. **기록의 증명 (Proof by Record)**
   - 화려한 애니메이션, 파티클, 그라데이션 남용 금지
   - 공식 기술 문서 또는 백서(Whitepaper)를 읽는 느낌의 정갈한 타이포그래피와 여백
   - 장식보다 정보 밀도와 가독성을 우선

2. **단일 소스 (One Source)**
   - 하나의 코드베이스로 **인터랙티브 웹**과 **고품질 PDF 이력서**를 동시에 지원
   - 콘텐츠를 웹용/PDF용으로 이중 관리하지 않음

3. **선택적 인터랙션 (Selective Interaction)**
   - 동적 컴포넌트는 설명이 복잡한 기술적 의사결정(예: E2EE 키 교환)에만 제한적으로 사용
   - "움직일 수 있으니까 움직이게 하는" 결정 금지. 인터랙션은 반드시 정보 전달을 돕는 경우에만 도입

---

## 2. 기술 스택

| 영역 | 기술 | 선택 이유 |
|---|---|---|
| Core | React + **Next.js (App Router)** | 서버 컴포넌트 기반 렌더링 최적화 역량 증명 |
| Styling | **Tailwind CSS** | `print:` 유틸리티로 웹/PDF 이중 지원이 압도적으로 편리 |
| Content | `next-mdx-remote` 또는 `contentlayer` | 옵시디언/마크다운 기반 회고·트러블슈팅 문서를 정적 페이지로 렌더링 |
| Deployment | **Vercel** | CI/CD 자동화, 빠른 배포 |

### 라이브러리 선택 규칙

- 신규 라이브러리 추가 전, 기존 스택(Tailwind, Next.js 내장 기능)으로 해결 가능한지 먼저 검토
- 애니메이션 라이브러리(framer-motion 등)는 **의사결정 카드에서 다이어그램을 다루는 경우에 한해서만** 도입 고려
- 무거운 UI 키트(MUI, Ant Design 등) 금지 — "정갈한 타이포그래피" 원칙과 충돌

---

## 3. 페이지 및 라우팅 구조

단일 페이지(One-Page) 이력서 형태를 유지하되, 깊이가 필요한 프로젝트는 별도 페이지로 분리합니다.

```
/
├── /                           # Home / Main Resume (원 페이지 이력서)
│   ├── Hero                    # 소개 카피, 이메일/GitHub, [PDF 다운로드(Ctrl+P 안내)] 버튼
│   ├── Skills                  # Frontend / Backend / Infra·Security 분류 + 활용 목적
│   └── Projects (Summary)      # 타임투게더, Monimentoom 3줄 요약 + Before/After 지표
│
├── /projects/timetogether      # 대표 프로젝트 1: 프론트/보안 딥다이브
│   ├── 클라이언트 아키텍처 다이어그램
│   ├── <DecisionCard> E2EE 적용 배경 및 상태 관리
│   └── (선택) E2EE 퍼블릭 키 교환 인터랙티브 다이어그램
│
├── /projects/monimentoom       # 대표 프로젝트 2: 백엔드/인프라 딥다이브
│   ├── EC2 + Nginx 인프라 아키텍처 다이어그램
│   ├── <DecisionCard> JPA N+1 해결
│   └── <DecisionCard> Blue-Green 무중단 배포
│
└── /log                        # 기술 블로그 / 회고
    └── 마크다운 기반 트러블슈팅 리스트
```

### 라우팅 원칙

- 메인(`/`)은 스크롤로 완결되는 이력서 — 불필요한 내부 네비게이션 추가 금지
- 프로젝트 상세 페이지는 "이력서에서 다 담기 어려운 깊이"가 명확할 때만 분리
- `/log` 리스트는 최신순, 태그 필터는 **과잉 기능**이므로 요청 전까지 구현하지 않음

---

## 4. 인쇄(PDF) 최적화 — 필수 준수 사항

**모든 페이지, 모든 컴포넌트 작성 시 "인쇄하면 어떻게 보일지"를 반드시 함께 고려합니다.**

### Tailwind `print:` 유틸리티 적용 규칙

| 대상 | 처리 |
|---|---|
| 네비게이션 바, 푸터, 페이지 이동 버튼, 다크모드 토글 | `print:hidden` |
| 전체 배경/텍스트 색상 | `print:bg-white print:text-black` (다크모드 상태라도 인쇄 시 강제 화이트) |
| 하이퍼링크 | CSS `::after`로 인쇄 시에만 URL 텍스트 노출 |
| 의사결정 카드, 주요 문단 | `print:break-inside-avoid` (페이지 중간 잘림 방지) |

### 하이퍼링크 전개 예시 (글로벌 CSS)

```css
@media print {
  a[href^="http"]::after {
    content: " (" attr(href) ")";
    font-size: 0.85em;
    color: #444;
  }
}
```

### 작업 체크리스트 (컴포넌트 추가/수정 시마다)

- [ ] 웹에서 잘 보이는가?
- [ ] `Ctrl + P` / `Cmd + P`로 프리뷰 시 불필요한 UI(버튼, 네비)가 숨겨지는가?
- [ ] 다크모드 상태에서 인쇄해도 흰 배경 + 검은 글씨로 나오는가?
- [ ] 카드/문단이 페이지 경계에서 잘리지 않는가?
- [ ] 링크의 실제 URL이 인쇄본에서 읽히는가?

---

## 5. 핵심 컴포넌트 표준: `<DecisionCard />`

프로젝트 상세 페이지의 **모든** '문제 해결' 경험은 아래 구조를 따르는 재사용 컴포넌트로 작성합니다. 자유 서술 금지 — 구조화된 논리 흐름으로 가독성과 면접 대비 효과를 동시에 확보합니다.

### Props 구조

```ts
interface DecisionCardProps {
  title: string;
  context: string;   // 상황: 어떤 비즈니스적/기술적 한계가 있었는가?
  problem: string;   // 문제: 그로 인해 발생한 구체적 문제(병목, 보안 취약점 등)
  action: string;    // 해결책: 어떤 기술(@EntityGraph, Supabase RPC 등)을 왜 선택했는가?
  result: string;    // 결과: 정량 지표(성능 % 향상, 다운타임 0 등)
  tags?: string[];   // 선택: 관련 기술 태그
}
```

### 작성 원칙

- **Context → Problem → Action → Result** 순서 고정. 섹션 순서 임의 변경 금지
- **Result는 반드시 정량 지표** 포함 (%, ms, 건수, 다운타임 시간 등). "체감상 빨라졌다" 같은 표현 불가
- **Action**에서는 선택한 기술뿐 아니라 "왜 그것을 선택했는지"를 함께 서술 (대안 비교 포함 권장)
- 카드 전체에 `print:break-inside-avoid` 적용

---

## 6. 콘텐츠 작성 톤 & 매너

- 1인칭 "저는" 대신 **간결한 서술형** ("담당하여 구현함", "개선하여 ~% 향상" 등)
- 과장된 표현("압도적", "혁신적") 금지 — 수치와 사실로 증명
- 기술 용어는 정확한 표기 유지: `Next.js`, `TypeScript`, `JPA`, `Nginx`, `EC2` (대소문자 주의)
- 영문 고유명사와 한글 사이에는 공백 (`Next.js 기반 클라이언트` ✅, `Next.js기반 클라이언트` ❌)

---

## 7. 디렉토리 구조 제안

```
/app
  /(main)                      # 메인 이력서 (레이아웃 공유)
    page.tsx
  /projects
    /timetogether/page.tsx
    /monimentoom/page.tsx
  /log
    page.tsx                   # 리스트
    /[slug]/page.tsx           # 상세
  layout.tsx                   # 루트 레이아웃 (글로벌 print CSS 포함)
  globals.css

/components
  /resume                      # Hero, Skills, ProjectSummary 등 이력서 전용
  /project                     # DecisionCard, ArchitectureDiagram 등
  /common                      # 공용 (네비, 푸터 등)

/content
  /log/*.mdx                   # 블로그/회고 마크다운

/lib
  mdx.ts                       # MDX 파싱 유틸
```

---

## 8. Claude Code 작업 시 체크리스트

새로운 기능 또는 페이지를 추가할 때 반드시 아래를 자문합니다.

1. **디자인 3원칙에 부합하는가?** (기록의 증명 / 단일 소스 / 선택적 인터랙션)
2. **인쇄(PDF)에서도 문제없이 렌더링되는가?**
3. **의사결정 경험이라면 `<DecisionCard />`를 사용했는가?**
4. **새 라이브러리를 추가했다면, 기존 스택으로 해결 불가능한 합당한 이유가 있는가?**
5. **App Router의 서버 컴포넌트를 우선 사용했는가?** (클라이언트 컴포넌트는 인터랙션이 필요한 최소 단위에만)
6. **정량 지표 없이 정성적 표현으로만 성과를 서술하고 있지 않은가?**

---

## 9. 하지 말아야 할 것 (Anti-Patterns)

- ❌ Hero 섹션에 풀스크린 그라데이션 배경 + 파티클 애니메이션
- ❌ 스크롤 트리거 등장 애니메이션 남발
- ❌ 이모지로 기술 스택 아이콘 대체 (정식 로고 사용 또는 텍스트)
- ❌ "저는 열정적이고 성실한 개발자입니다" 류의 서사형 자기소개
- ❌ 웹 전용 컴포넌트(동영상, 인터랙티브 위젯)를 PDF 대체 없이 삽입
- ❌ 프로젝트 회고를 자유 서술로 작성 (반드시 `<DecisionCard />` 구조 사용)